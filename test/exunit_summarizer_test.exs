defmodule TestWithSummaryTest do
  use ExUnit.Case

  test "one that passes" do
    assert 1 == 1
  end

  describe "1" do
    test "one that fails" do
      assert 1 == 2
    end

    test "one that fails2" do
      assert 1 == 2
    end

    test "one that fai4ls" do
      assert 1 == 2
    end

    test "one that fail5s" do
      assert 1 == 2
    end

    test "one that fails6" do
      assert 1 == 2
    end

    test "one th7at fails" do
      assert 1 == 2
    end

    test "one tha8t fails" do
      assert 1 == 2
    end

    test "one that9 fails" do
      assert 1 == 2
    end

    test "one that 1fails" do
      assert 1 == 2
    end

    test "one that f2ails" do
      assert 1 == 2
    end

    test "one that fa3ils" do
      assert 1 == 2
    end

    test "one that fail4s" do
      assert 1 == 2
    end
  end

  describe "2" do
    test "one that fails" do
      assert 1 == 2
    end

    test "one that fails2" do
      assert 1 == 2
    end

    test "one that fai4ls" do
      assert 1 == 2
    end

    test "one that fail5s" do
      assert 1 == 2
    end

    test "one that fails6" do
      assert 1 == 2
    end

    test "one th7at fails" do
      assert 1 == 2
    end

    test "one tha8t fails" do
      assert 1 == 2
    end

    test "one that9 fails" do
      assert 1 == 2
    end

    test "one that 1fails" do
      assert 1 == 2
    end

    test "one that f2ails" do
      assert 1 == 2
    end

    test "one that fa3ils" do
      assert 1 == 2
    end

    test "one that fail4s" do
      assert 1 == 2
    end
  end

  describe "3" do
    test "one that fails" do
      assert 1 == 2
    end

    test "one that fails2" do
      assert 1 == 2
    end

    test "one that fai4ls" do
      assert 1 == 2
    end

    test "one that fail5s" do
      assert 1 == 2
    end

    test "one that fails6" do
      assert 1 == 2
    end

    test "one th7at fails" do
      assert 1 == 2
    end

    test "one tha8t fails" do
      assert 1 == 2
    end

    test "one that9 fails" do
      assert 1 == 2
    end

    test "one that 1fails" do
      assert 1 == 2
    end

    test "one that f2ails" do
      assert 1 == 2
    end

    test "one that fa3ils" do
      assert 1 == 2
    end

    test "one that fail4s" do
      assert 1 == 2
    end
  end
end
